<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainMenu"
    android:orientation="vertical"
    android:background="@drawable/backgroundbeta">

    <!-- NOTE: android:theme seems to make the app throw exceptions on button press -->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textSize="72sp"
        android:text="@string/app_name"
        android:id="@+id/textView"
        android:textColor="@color/white"
        android:layout_marginTop="5dp"
        android:layout_centerHorizontal="true"/>


    <Button
        android:layout_width="200dp"
        android:layout_height="56dp"
        android:text="@string/view_list"
        android:id="@+id/viewList"
        android:layout_marginTop="100dp"
        android:textSize="20sp"
        android:background="@color/metalblue"
        android:textColor="@color/darkblue"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true"
        android:onClick="viewList"/>


    <Button
        android:layout_width="wrap_content"
        android:layout_height="56dp"
        android:text="@string/add_to_list"
        android:id="@+id/addList"
        android:layout_marginTop="26dp"
        android:textSize="20sp"
        android:background="@color/metalblue"
        android:textColor="@color/darkblue"
        android:layout_below="@id/viewList"
        android:layout_alignLeft="@id/viewList"
        android:layout_alignRight="@id/viewList"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="26dp"
        android:onClick="addToGiftList"/>


</RelativeLayout>
